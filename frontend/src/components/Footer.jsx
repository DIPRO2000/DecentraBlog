import React from "react";

const Footer = () => (
    <footer className="mt-5 pb-8 text-center text-slate-500 text-sm border-t border-slate-800 pt-8">
        <p>&copy; {new Date().getFullYear()} DecentraBlog. A decentralized future built on transparency.</p>
    </footer>
);

export default Footer